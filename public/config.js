// Runtime configuration — edited directly on the server, not baked into the bundle.
// On the server replace empty strings with real values:
//   SUPABASE_URL: 'https://<your-project>.supabase.co'
//   SUPABASE_KEY: 'eyJ...' or 'sb_publishable_...'
// Reload the browser — no rebuild required.
window.__APP_CONFIG__ = {
  SUPABASE_URL: '',
  SUPABASE_KEY: '',
}
